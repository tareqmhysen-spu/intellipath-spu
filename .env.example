# =============================================================================
# IntelliPath Environment Configuration | إعدادات بيئة IntelliPath
# =============================================================================
# Copy this file to .env and fill in your values
# انسخ هذا الملف إلى .env واملأ القيم الخاصة بك
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration | إعدادات Supabase
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# -----------------------------------------------------------------------------
# AI Gateway Configuration | إعدادات بوابة الذكاء الاصطناعي
# Used by Edge Functions | تُستخدم في Edge Functions
# -----------------------------------------------------------------------------
AI_API_KEY=your-ai-api-key
AI_GATEWAY_URL=https://api.openai.com/v1

# -----------------------------------------------------------------------------
# Neo4j Graph Database (Optional) | قاعدة بيانات الرسم البياني
# Used for course prerequisites and relationships
# تُستخدم للمتطلبات المسبقة وعلاقات المقررات
# -----------------------------------------------------------------------------
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-password

# -----------------------------------------------------------------------------
# Qdrant Vector Database (Optional) | قاعدة البيانات المتجهة
# Used for semantic search and RAG
# تُستخدم للبحث الدلالي ونظام RAG
# -----------------------------------------------------------------------------
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=your-qdrant-api-key

# -----------------------------------------------------------------------------
# OpenAI (for embeddings) | OpenAI للتضمينات
# Used by Python scripts for document processing
# تُستخدم في سكربتات Python لمعالجة المستندات
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your-openai-api-key

# -----------------------------------------------------------------------------
# Redis Cache (Optional) | ذاكرة التخزين المؤقت
# Used for query caching and rate limiting
# تُستخدم للتخزين المؤقت والحد من الطلبات
# -----------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379

# =============================================================================
# IMPORTANT SECURITY NOTES | ملاحظات أمنية مهمة
# =============================================================================
# 1. NEVER commit the actual .env file to version control
#    لا ترفع ملف .env الحقيقي أبداً إلى Git
#
# 2. Keep SUPABASE_SERVICE_ROLE_KEY secret (backend only)
#    احتفظ بـ SERVICE_ROLE_KEY سرياً (للباكند فقط)
#
# 3. Rotate API keys periodically
#    غيّر مفاتيح API بشكل دوري
#
# 4. Use environment variables in production
#    استخدم متغيرات البيئة في الإنتاج
# =============================================================================
